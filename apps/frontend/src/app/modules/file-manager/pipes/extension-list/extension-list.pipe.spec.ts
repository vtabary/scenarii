import { ExtensionListPipe } from './extension-list.pipe';

describe('ExtensionListPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtensionListPipe();
    expect(pipe).toBeTruthy();
  });
});
